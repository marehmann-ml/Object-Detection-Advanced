# Source Code

This folder contains the main source code used throughout the object detection workflow.

## Structure

- `configs/` — Dataset, class mapping, and training configuration files.
- `training/` — Model training, fine-tuning, and validation scripts.
- `inference/` — Image and video inference scripts used to evaluate trained models.

The source code is organized to keep configuration, training, and inference workflows separate and easy to maintain.
